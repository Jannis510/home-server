---
name: Smoke

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "compose.yml"
      - "compose.ci.yml"
      - "config/**"
      - "scripts/**"
  push:
    branches: [main]
    paths:
      - "compose.yml"
      - "compose.ci.yml"
      - "config/**"
      - "scripts/**"

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Prepare CI-only local files
        run: |
          mkdir -p config/stepca config/traefik
          mkdir -p artifacts/pki
          chmod 0777 artifacts/pki
          printf '%s\n' 'ci-smoke-password' > config/stepca/password.txt
          printf '%s\n' 'admin:$2y$05$wR35Tr0xI6qG1w0vP7YfbuKNhUJgOuA7nA6cLxgbmRZwFz7wXfQhW' > config/traefik/usersfile

      - name: Bring stack up
        run: |
          docker compose --env-file .env.ci -f compose.yml -f compose.ci.yml up -d --build

      - name: Run smoke script
        run: |
          chmod +x scripts/smoke.sh
          scripts/smoke.sh

      - name: Dump compose ps on failure
        if: failure()
        run: |
          docker compose --env-file .env.ci -f compose.yml -f compose.ci.yml ps
          docker compose --env-file .env.ci -f compose.yml -f compose.ci.yml logs --no-color --tail=200
